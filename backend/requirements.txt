# ================================================================
# AiDoktor Backend — bog'liqliklar
# Python 3.11+
#
# Hamma versiya QAT'IY qadalgan (==): bir xil commit har doim bir xil
# kodni o'rnatadi. Ilgari bir qismi `>=` edi — bu bir xil commitdan
# turli kunlarda turli paket to'plami chiqishiga olib kelardi.
#
# Bu versiyalar CI va lokal testdan o'tgan versiyalar.
# ================================================================

# -- Django yadrosi ----------------------------------------------
# 5.0 seriyasi 2025-yil aprelda qo'llab-quvvatlashdan chiqqan (EOL) va
# unda tuzatilmagan zaifliklar bor. 5.2 — LTS.
Django==5.2.14
django-jazzmin==3.0.1
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-environ==0.11.2
djangorestframework-simplejwt==5.5.1
python-decouple==3.8

# -- Ma'lumotlar bazasi ------------------------------------------
psycopg2-binary==2.9.11

# -- Tasvir ------------------------------------------------------
Pillow==12.3.0

# -- API va filtrlar ---------------------------------------------
django-filter==24.3
drf-yasg==1.21.7

# -- Statik fayllar ----------------------------------------------
whitenoise==6.9.0

# -- Production server -------------------------------------------
# 21.2.0 da HTTP request smuggling zaifligi bor edi (nginx orqasida
# so'rovni autentifikatsiyadan yashirincha o'tkazish mumkin edi).
gunicorn==23.0.0

# -- HTTP --------------------------------------------------------
# 2.31.0 da bir marta verify=False qilingan sessiya keyingi so'rovlarda
# ham TLS tekshiruvini jimgina o'tkazib yuborardi.
requests==2.34.2

# -- Kesh va navbat ----------------------------------------------
redis==5.0.1
django-redis==5.4.0
celery==5.3.4

# -- Kriptografiya -----------------------------------------------
cryptography==50.0.0

# -- Yordamchi ---------------------------------------------------
setuptools==82.0.0
openpyxl==3.1.5
reportlab==4.4.10
pymupdf==1.27.2.2

# -- AI ----------------------------------------------------------
# Matn chaqiruvlari OpenAI-mos API orqali ketadi (claude_utils.py,
# azure_utils.py). `anthropic` paketi olib tashlandi — kodda hech qayerda
# import qilinmagan edi.
openai==1.109.1
