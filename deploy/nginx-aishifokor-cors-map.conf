# /etc/nginx/conf.d/aishifokor-cors-map.conf — http kontekstida yuklanadi
map $http_origin $aishifokor_cors_origin {
    default "";
    "https://aishifokor.uz" $http_origin;
    "https://www.aishifokor.uz" $http_origin;
    "http://aishifokor.uz" $http_origin;
}
